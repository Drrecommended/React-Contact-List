{/* <div className="App">
      <Router>
        <div class="phonebook">
          <div class="view">List View</div>
          <div>
            <div class="title">My Peeps</div>
            {contacts.map(item => {
              return <div className="name"><Link to={`/contacts/${item.id}`}>{item.name.first}</Link></div>
            })}
           
          </div>
        </div>
      </Router> 
    </div> */}
